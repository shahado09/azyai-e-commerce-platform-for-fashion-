<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>

  <%- include('../partials/_navbar.ejs') %>

  <div class="page">
    <div class="form-card">
      <h1 class="form-title">Edit Cloth</h1>

      <form class="form" action="/cloth/<%= foundCloth._id %>?_method=PUT" method="POST" enctype="multipart/form-data">

        <div class="form-group">
          <label class="form-label" for="name">Name</label>
          <input class="form-input" type="text" id="name" name="name" value="<%= foundCloth.name %>">
        </div>

        <div class="form-group">
          <label class="form-label" for="description">Description</label>
          <input class="form-input" type="text" id="description" name="description" value="<%= foundCloth.description %>">
        </div>

        <div class="form-group">
          <label class="form-label" for="price">Price (BD)</label>
          <input class="form-input" type="text" id="price" name="price" step="0.1" value="<%= foundCloth.price %>">
        </div>

        <div class="sizes-row">
          <label class="form-label">Available Sizes:</label>

          <label class="form-label">
            <input class="form-input" type="checkbox" name="sizes" value="XS"
              <%= (foundCloth.sizes && foundCloth.sizes.includes("XS")) ? "checked" : "" %> >
            XS
          </label>

          <label class="form-label">
            <input class="form-input" type="checkbox" name="sizes" value="S"
              <%= (foundCloth.sizes && foundCloth.sizes.includes("S")) ? "checked" : "" %> >
            S
          </label>

          <label class="form-label">
            <input class="form-input" type="checkbox" name="sizes" value="M"
              <%= (foundCloth.sizes && foundCloth.sizes.includes("M")) ? "checked" : "" %> >
            M
          </label>

          <label class="form-label">
            <input class="form-input" type="checkbox" name="sizes" value="L"
              <%= (foundCloth.sizes && foundCloth.sizes.includes("L")) ? "checked" : "" %> >
            L
          </label>

          <label class="form-label">
            <input class="form-input" type="checkbox" name="sizes" value="XL"
              <%= (foundCloth.sizes && foundCloth.sizes.includes("XL")) ? "checked" : "" %> >
            XL
          </label>
        </div>

        <div class="form-group">
          <label class="form-label" for="images">Upload Imges</label>
          <input class="form-input" type="file" id="images" name="images" multiple>
          <p class="form-hint">* If you upload new images, they will replace the old ones.</p>
        </div>

        <div class="form-check">
          <label class="form-label" for="isAvailable">Is Available</label>
          <input class="form-input" type="checkbox" id="isAvailable" name="isAvailable"
            <%= foundCloth.isAvailable ? "checked" : "" %> >
        </div>

        <div class="form-actions">
          <button class="Create-btn" type="submit">Update</button>
          <a class="Cancel-btn" href="/cloth/<%= foundCloth._id %>">Cancel</a>
        </div>

      </form>

    </div>
  </div>

</body>
</html>